<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.jbosworth2.japanese_studies"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KanjiActivity"
            android:label="@string/kanji" />
        <activity
            android:name=".KanjiReadingActivity" />
        <activity
            android:name=".KanjiWritingActivity" />
        <activity
            android:name=".VocabActivity"
            android:label="@string/vocab" />
        <activity
            android:name=".VocabStartReadingActivity" />
        <activity
            android:name=".VocabReadingActivity" />
        <activity
            android:name=".VocabListeningActivity" />
    </application>

</manifest>
